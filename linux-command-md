q	# comman-md  
       **id** // informace o uzivateli
       **ls**// list
       **ls-l** // detailnejsi list
       **cd**// change directory
       **pwd** // print
       **man** // manualove stranky
       **df** // vyuziti filesystemu
       **df-h** // data prepise do citelnejsich jednotek
       **ps** // vypise bezici procesy
       **ps -ef|grep michal** // 
       **grep** // vyhledava v textu
       **cat** // vypise obsah souboru
       **more** // vypise obsah souboru
       **less** // vypise obsah souboru
       **ip** a // moje ip 
       **ip r** // rootovaci tabulka
       **su** // switch user 
       **su -l -root**  // prepnuti na uyivatele root
       **ssh -l jmeno ip** // prepnuti na vydaleneho uyivatele
